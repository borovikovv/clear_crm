import { useState, useEffect } from "react";
import Loading from "../login/Login";

import s from "./profile.module.css";

const Profile = (props) => {
    return (
        <div classNames={s.profile}>
            Profile
        </div>
    )
}; 

export default Profile;